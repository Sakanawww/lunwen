# 课程助教系统 UI 设计提示词

> 用于指导 Agent 进行前端界面优化，参考行业顶尖设计（ChatGPT、Claude、Notion AI）

---

## 📋 项目背景

**系统名称**：基于 Agent 的课程助教系统  
**技术栈**：FastAPI + Jinja2 + 原生 JS（禁用 Vue/React）  
**核心功能**：智能答疑、作业批改、试题生成、知识库管理

---

## 🎨 一、整体设计规范

### 1. 配色方案（深色科技主题）

```css
:root {
  /* 背景色 */
  --bg-page: #0F172A;           /* 深蓝黑页面背景 */
  --bg-sidebar: #1E293B;        /* 侧栏/卡片背景 */
  --bg-card: #1E293B;
  --bg-input: #334155;          /* 输入框背景 */
  
  /* 文字色 */
  --text-primary: #F8FAFC;      /* 主文字 */
  --text-secondary: #94A3B8;    /* 次要文字 */
  --text-muted: #64748B;        /* 弱化文字 */
  
  /* 强调色 */
  --accent-blue: #3B82F6;       /* 科技蓝 */
  --accent-purple: #8B5CF6;     /* 紫色 */
  --accent-cyan: #06B6D4;       /* 青色高亮 */
  
  /* 渐变 */
  --gradient-primary: linear-gradient(135deg, #3B82F6 0%, #8B5CF6 100%);
  --gradient-glow: radial-gradient(circle, rgba(59,130,246,0.15) 0%, transparent 70%);
  
  /* 边框与阴影 */
  --border-subtle: rgba(148, 163, 184, 0.1);
  --shadow-glow: 0 0 20px rgba(59, 130, 246, 0.3);
}
```

### 2. 字体规范

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap');

--font-sans: 'Inter', -apple-system, 'Microsoft YaHei', sans-serif;
--font-mono: 'JetBrains Mono', 'Consolas', monospace;
```

### 3. 图标库

```html
<!-- 引入 Remix Icon -->
<link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
```

| 功能 | 图标类名 |
|------|----------|
| 发送 | `ri-send-plane-fill` |
| 附件 | `ri-attachment-2` |
| 知识库 | `ri-folder-cloud-line` |
| 批改 | `ri-checkbox-circle-line` |
| 出题 | `ri-edit-circle-line` |
| 看板 | `ri-dashboard-3-line` |
| 设置 | `ri-settings-4-line` |
| 退出 | `ri-logout-circle-line` |

---

## 🏗️ 二、页面布局规范

### 1. 智能答疑页（核心页面）

```
┌─────────────────────────────────────────────────────────────────────┐
│ 顶部导航栏 (深色 #1E293B, 60px 高)                                    │
│ [Logo] 课程助教系统                              [用户] [退出]        │
├──────────┬──────────────────────────────────────────────┬────────────┤
│ 会话列表  │              对话主区域                      │ 知识溯源   │
│ (280px)  │              (自适应)                        │  (280px)   │
│          │                                              │            │
│ [新建对话]│  ┌────────────────────────────────────┐    │ 引用来源   │
│ ──────── │  │  欢迎使用智能答疑                   │    │ ───────── │
│ 历史会话 │  │                                      │    │ 📄 文档 1  │
│ 1        │  │  💬 用户消息 (右对齐，蓝色气泡)      │    │ 📄 文档 2  │
│ 2        │  │  🤖 AI 回复 (左对齐，带引用卡片)      │    │ ...      │
│ 3        │  │                                      │    │            │
│          │  └────────────────────────────────────┘    │            │
│          │  ┌────────────────────────────────────┐    │            │
│          │  │ [输入框]                    [发送] │    │            │
│          │  └────────────────────────────────────┘    │            │
└──────────┴──────────────────────────────────────────────┴────────────┘
```

**关键样式要求：**
- 会话列表：深色背景，当前会话蓝色高亮 + 左侧 3px 边框
- 消息气泡：圆角 18px，用户消息渐变蓝，AI 消息白色边框
- 引用卡片：浅灰背景，可展开/收起，点击跳转
- 输入框：最小高度 56px，Focus 蓝色光晕
- 动画：消息滑入 + 淡入，流式输出打字机效果

---

### 2. 登录页

```
┌─────────────────────────────────────────────────────────────────────┐
│ [Logo] 课程助教系统                                    [登录]       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                    ┌─────────────────────┐                          │
│                    │   🎓 课程助教系统    │                          │
│                    │  Agent 驱动·答疑·批改·出题 │                  │
│                    ├─────────────────────┤                          │
│                    │ 用户名 [👤 _______] │                          │
│                    │ 密码   [🔒 _______] │                          │
│                    │ [    登  录    ]    │                          │
│                    │ 还没有账号？注册    │                          │
│                    ├─────────────────────┤                          │
│                    │ 演示账号：student/teacher/admin │              │
│                    │ 密码均为 123456          │                      │
│                    └─────────────────────┘                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**样式要求：**
- 背景：渐变 (#667eea → #764ba2) + 网格纹理
- 卡片：白色背景，圆角 16px，阴影 `0 20px 40px rgba(0,0,0,0.2)`
- 输入框：浅蓝背景，Focus 蓝色边框
- 按钮：渐变背景，hover 上浮 2px

---

### 3. 管理员控制台

```
┌─────────────────────────────────────────────────────────────────────┐
│ 管理员控制台                                                         │
│ Agent 编排配置与系统审计日志                                          │
├─────────────────────────────────────────────────────────────────────┤
│ [Agent 配置 3]  [系统日志 0]   ← Tab 切换                           │
├─────────────────────────────────────────────────────────────────────┤
│ Agent 配置                                      [编排设置]            │
│ ┌─────────────────────────────────────────────────────────────────┐ │
│ │ Agent  │ 角色职责 │ 模型 │ 状态 │ 路由关键词 │                  │
│ ├─────────────────────────────────────────────────────────────────┤ │
│ │ 答疑   │ 面向学生...│ qwen │ ●启用│ [答疑][问题]...│            │ │
│ │ 批改   │ 面向教师...│ qwen │ ●启用│ [批改][评分]...│            │ │
│ │ 出题   │ 面向教师...│ qwen │ ●启用│ [出题][测验]...│            │ │
│ └─────────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│ 系统日志                                                             │
│ ┌─────────────────────────────────────────────────────────────────┐ │
│ │ [🔍 搜索用户/动作/详情...]                        [每页 10 ▼]    │ │
│ ├─────────────────────────────────────────────────────────────────┤ │
│ │ ID │ 用户 │ 操作 │ 详情 │ 时间 │                              │ │
│ │    │      │      │      │      │  [空状态图标] 暂无日志       │ │
│ └─────────────────────────────────────────────────────────────────┘ │
│                                        [< 1/1 >]                    │
└─────────────────────────────────────────────────────────────────────┘
```

**优化要点：**
- 表格行高：2.5rem（当前过高，压缩）
- 状态 Badge：小圆点 + 文字（绿色启用/灰色禁用）
- 关键词列：默认显示前 3 个，"+N 展开"
- 搜索框与分页器并排

---

### 4. 知识库管理页

```
┌─────────────────────────────────────────────────────────────────────┐
│ 📁 课程知识库管理 (RAG)                                              │
├─────────────────────────────────────────────────────────────────────┤
│ 选择课程：[数据结构 ▼]                                               │
│ 上传资料：[选择文件] 未选择文件    [上传并向量化]                     │
│          (支持 .txt / .md / .pdf)                                    │
├─────────────────────────────────────────────────────────────────────┤
│ 已入库文档                                                           │
│ ┌─────────────────────────────────────────────────────────────────┐ │
│ │ ID │ 标题 │ 知识块数 │ 上传时间 │ 操作 │                        │ │
│ ├─────────────────────────────────────────────────────────────────┤ │
│ │ 7  │ 数据结构讲义 │ 2 │ 2026-09-05 21:16 │ [查看] [删除] │      │ │
│ └─────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

**优化要点：**
- 上传区改为横向布局（课程 + 文件 + 按钮 一行）
- 文档列表增加删除按钮（红色）
- 支持拖拽上传，显示进度条

---

### 5. 数据看板页

```
┌─────────────────────────────────────────────────────────────────────┐
│ 📊 学情数据看板                                                       │
├─────────────────────────────────────────────────────────────────────┤
│ 选择课程：[数据结构 ▼]                                               │
├─────────────────────────────────────────────────────────────────────┤
│ 课程概览                                                             │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐             │
│ │  1   │ │  1   │ │  7   │ │ 85.0 │ │  2   │ │  1   │             │
│ │选课人数│ │作业提交│ │已批改 │ │平均分 │ │试题数 │ │知识库 │             │
│ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘             │
│ ┌──────┐                                                             │
│ │  30  │                                                             │
│ │答疑消息│                                                            │
│ └──────┘                                                             │
└─────────────────────────────────────────────────────────────────────┘
```

**优化要点：**
- 改为 4 列网格布局（避免单行过挤）
- 卡片宽 160px，数字 2rem 蓝色，标签灰色小字
- 卡片悬停：轻微上浮 + 阴影加深

---

## 🎬 三、动效规范

### 1. 消息出现动画

```css
@keyframes slideInRight {
  from { opacity: 0; transform: translateX(30px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes slideInLeft {
  from { opacity: 0; transform: translateX(-30px); }
  to { opacity: 1; transform: translateX(0); }
}

.message-user { animation: slideInRight 0.4s ease-out; }
.message-ai { animation: slideInLeft 0.4s ease-out; }
```

### 2. 流式输出效果

- AI 回复时显示打字机效果
- 光标闪烁：`border-right: 2px solid var(--accent-blue)`
- 逐字显示，每字延迟 20-50ms

### 3. 加载状态

```
🤖 思考中
▓▓▓▓▓▓▓░░░░░ 60%
正在检索知识库...
```

- 进度条：渐变填充，圆角
- 脉冲动画：`animation: pulse 1.5s infinite`

---

## 📐 四、间距与尺寸规范

### 1. 间距变量

```css
--spacing-xs: 0.5rem;   /* 8px */
--spacing-sm: 0.75rem;  /* 12px */
--spacing-md: 1rem;     /* 16px */
--spacing-lg: 1.5rem;   /* 24px */
--spacing-xl: 2rem;     /* 32px */
```

### 2. 圆角规范

```css
--radius-sm: 6px;   /* 输入框、小按钮 */
--radius-md: 10px;  /* 卡片 */
--radius-lg: 16px;  /* 大卡片、弹窗 */
--radius-xl: 24px;  /* 消息气泡 */
```

### 3. 组件尺寸

| 组件 | 高度 | 说明 |
|------|------|------|
| 导航栏 | 60px | 深色背景 |
| 输入框 | 48px | 单行 |
| 多行输入 | 56px min | 可自动扩展 |
| 按钮（大） | 48px | 主要操作 |
| 按钮（小） | 36px | 次要操作 |
| 表格行 | 48px | 标准行高 |

---

## ♿ 五、可访问性要求

1. **键盘导航**
   - Tab 键可遍历所有交互元素
   - Enter 发送消息，Shift+Enter 换行
   - Esc 关闭弹窗/侧栏

2. **屏幕阅读器**
   - 所有图标按钮添加 `aria-label`
   - 消息区域用 `role="log"`
   - 加载状态用 `aria-live="polite"`

3. **对比度**
   - 文字与背景对比度 ≥ 4.5:1（WCAG AA）
   - 避免纯黑 (#000000)，用 #1E293B

4. **焦点可见**
   - Focus 状态：`outline: 2px solid var(--accent-blue)`
   - Focus 光晕：`box-shadow: 0 0 0 3px rgba(59,130,246,0.3)`

---

## 📱 六、响应式断点

| 断点 | 宽度 | 布局调整 |
|------|------|----------|
| 桌面 | >1024px | 三栏全显示 |
| 平板 | 768-1024px | 隐藏溯源侧栏，按钮展开 |
| 手机 | <768px | 仅对话区，侧栏抽屉式 |

---

## 🎯 七、参考设计来源

| 产品 | 借鉴点 |
|------|--------|
| ChatGPT | 会话列表布局、输入区设计 |
| Claude | 引用卡片样式、反馈按钮 |
| Notion AI | 深色主题、渐变按钮 |
| 通义千问 | 欢迎状态设计、建议问题 |
| Poe | 流式输出动效、代码高亮 |
| Linear | 表格设计、快捷键提示 |

---

## 📦 八、输出要求

1. **优先实现页面**：
   - [ ] 智能答疑页（核心）
   - [ ] 登录页
   - [ ] 管理员控制台

2. **代码规范**：
   - 语义化 HTML 标签
   - CSS 用变量，方便主题切换
   - JS 模块化，SSE 流式接收逻辑分离

3. **文件结构**：
   ```
   app/templates/
   ├── base.html          # 基础模板
   ├── login.html         # 登录页
   ├── chat.html          # 智能答疑（重点）
   ├── dashboard.html     # 管理员控制台
   ├── kb.html            # 知识库管理
   ├── grading.html       # 作业批改
   └── questions.html     # 试题生成
   ```

4. **CSS 组织**：
   ```
   app/static/
   ├── css/
   │   ├── variables.css   # CSS 变量定义
   │   ├── base.css        # 基础样式
   │   ├── chat.css        # 答疑页专用
   │   └── dashboard.css   # 控制台专用
   └── js/
       ├── main.js         # 公共逻辑
       └── chat.js         # 答疑页 SSE 逻辑
   ```

---

## ⚡ 九、快速验证方案

如需快速出效果，可先执行：

1. 保持现有 HTML 结构
2. 新增 `dark-theme.css` 覆盖样式
3. 仅修改关键样式：
   - 消息气泡圆角 + 渐变
   - 输入框 Focus 光晕
   - 按钮渐变背景
   - 表格行高压缩

**1 小时可见效的改动清单：**
- [ ] 全局配色切换为深色主题
- [ ] 消息气泡改为圆角 + 渐变
- [ ] 输入框增加 Focus 光晕
- [ ] 按钮改为渐变背景
- [ ] 表格行高从 3rem 压缩到 2.5rem
- [ ] 图标统一换为 Remix Icon

---

> **最后更新**：2025 年  
> **适用项目**：《基于 Agent 的课程助教系统设计与实现》

# 功能扩展方案评估文档

> 背景：指导老师反馈"三 Agent 答疑/批改/出题闭环功能单薄"，建议对照学习通补充班级管理、平时表现评估、考勤等功能并做数据可视化。
> 本文档为方案评估稿，确认后据此实现。

---

## 一、现状分析

### 已有功能（不止三个 Agent）

| 模块 | 现状 |
|------|------|
| 三 Agent 闭环 | 答疑 / 批改 / 出题 ✅ |
| RAG 知识库 | FAISS + Qwen Embedding + 引用溯源 ✅ |
| LangGraph 编排 | 有状态多 Agent 工作流 ✅ |
| 课程管理 | CRUD + RBAC 成员管理 ✅ |
| 学情看板 | 趋势/完成度/知识库/热门问题四图表 ✅ |
| 作业全链路 | 发布→提交→AI 批改→统计 ✅ |
| 学生名单、练习、试题回收站、管理后台 | ✅ |

### 真正的缺口（对照学习通）

| 学习通功能 | 现状 | 缺口 |
|------------|------|------|
| 考勤管理 | ❌ 无表无接口 | **全缺** |
| 平时表现评估 | ⚠️ 有看板指标无"平时分"汇总 | 缺综合评分 |
| 班级管理 | ⚠️ `class_name` 是裸字符串 | 缺班级实体 |
| 课程公告 | ❌ 无 | 缺 |

### 问题的两层根因

1. **叙事层**：论文/框架说明文档第一句就把自己框死在"三 Agent 答疑/批改/出题闭环"，没把已有 LMS 能力讲出来。
2. **功能层**：确实缺考勤、平时分、班级等教务维度。

---

## 二、功能范围（确认全做 4 项）

| 优先级 | 模块 | 论文价值 |
|--------|------|----------|
| P0 | 考勤管理 + 预警 Agent | ★★★★★ |
| P0 | 平时表现评估 + 学情分析 Agent | ★★★★★ |
| P1 | 班级管理 | ★★ |
| P2 | 课程公告 / 通知 | ★ |

**核心原则**：加功能的同时必须保住"Agent"主题，让智能体参与新流程，而非单纯加 CRUD 教务模块（否则沦为普通 LMS，丢了差异化）。

---

## 三、各模块设计决策

### 3.1 班级管理

**决策：独立实体 + 双关联**

- 新建 `classes` 表（id / name / 年级 / 专业）
- 班级 ↔ 学生：一对多（`students.class_name` → `class_id` 外键迁移）
- 班级 ↔ 课程：多对多（授课班级，新建 `class_courses` 关联表）
- 考勤可按课程发起，平时分看板可按班级过滤

**影响**：schema 改动最大（加 2 张表 + 改学生表），但最贴近学习通，数据维度完整。

### 3.2 考勤管理 + 预警 Agent

**数据模型：课次 + 明细双表**

```
attendance_sessions（考勤课次）
  - id
  - course_id        外键→courses
  - class_id         外键→classes（可选，按班级发起）
  - session_date     日期
  - status           open / closed
  - created_by       外键→users（发起教师）

attendance_records（签到明细）
  - id
  - session_id       外键→attendance_sessions
  - student_id       外键→users
  - status           ENUM('present','late','leave','absent')
  - signed_at        签到时间
  - UNIQUE(session_id, student_id)
```

**功能流程**：
- 教师在考勤页发起"考勤课次"→ 学生端签到 → 教师可手动修正状态
- 出勤率可视化：趋势线、缺勤排行榜、按周热力图

**预警 Agent 触发：教师手动触发 + 确认**

- 考勤页"生成预警"按钮 → LLM 对连续缺勤 ≥ 3 次的学生生成自然语言提醒文案
- 教师确认后入库展示（演示中不真实推送）
- 论文可写"人机协同预警"，避免自动预警的噪声问题

### 3.3 平时表现评估 + 学情分析 Agent

**平时分算法：固定权重四维**

```
平时分 = 出勤率   × 0.30
      + 作业完成率 × 0.30
      + 练习正确率 × 0.20
      + 答疑活跃度 × 0.20
```

- 权重固定写在配置里，简单可解释
- 看板展示每个维度子分 + 总分 + 雷达图（多维度）
- 学生排名表

**存储**：`performance_scores` 表物化存储四维子分 + 总分 + 计算时间（便于历史对比，避免每次实时聚合）

**学情分析 Agent 交付：看板内嵌诊断面板**

- 看板新增"学情诊断"按钮 → 后端调用 LLM，传入某学生多维数据 → SSE 流式返回一段自然语言诊断 + 改进建议
- 复用现有 SSE 流式架构，成本最低
- 学生端也能查看自己的诊断报告
- **差异化亮点**：学习通只给数字不给诊断，本系统用 LLM 生成自然语言分析——这是论文创新点

### 3.4 课程公告 / 通知

- `announcements` 表（course_id / title / content / created_by / created_at）
- 简单 CRUD + 前端通知中心
- 工作量小，锦上添花

---

## 四、Agent 扩充（3 → 5）

| Agent | 职责 | 现有/新增 |
|-------|------|-----------|
| 答疑 Agent | RAG 检索 + 引用溯源答疑 | 现有 |
| 批改 Agent | 作业评分 + 评语生成 | 现有 |
| 出题 Agent | 按知识点/难度生成试题 | 现有 |
| **考勤预警 Agent** | 对连续缺勤学生生成自然语言提醒 | **新增** |
| **学情分析 Agent** | 多维数据 → 自然语言诊断 + 改进建议 | **新增** |

两个新 Agent 复用现有 LangGraph 编排框架，路由关键词扩展：

```python
ROUTE_KEYWORDS = {
    "答疑Agent": [...],
    "批改Agent": [...],
    "出题Agent": [...],
    "考勤预警Agent": ["缺勤", "旷课", "考勤", "出勤率", "预警", "提醒"],
    "学情分析Agent": ["学情", "表现", "诊断", "平时分", "建议", "分析报告"],
}
```

---

## 五、数据库变更总览

### 新增表（6 张）

| 表名 | 用途 |
|------|------|
| `classes` | 班级实体 |
| `class_courses` | 班级 ↔ 课程多对多 |
| `attendance_sessions` | 考勤课次 |
| `attendance_records` | 签到明细 |
| `performance_scores` | 平时分四维子分 + 总分 |
| `announcements` | 课程公告 |

### 修改表（1 张）

- `students`：`class_name VARCHAR(50)` → `class_id BIGINT` 外键 → `classes.id`

### 同步改动文件

| 文件 | 改动 |
|------|------|
| `database/schema.sql` | 新增 6 张表 DDL + 改 students 表 |
| `app/models/models.py` | 新增 6 个 ORM 模型 + 改 Student 模型 |
| `app/schemas/schemas.py` | 新增请求/响应 schema |
| `scripts/seed_demo.py` | 补班级 + 考勤 + 平时分 + 公告演示数据 |
| `database/migrations/` | 新增幂等迁移 SQL + README |

---

## 六、后端新增接口

### 新增 API 文件（3 个）

| 文件 | 路由 | 说明 |
|------|------|------|
| `app/api/attendance.py` | `/api/attendance/...` | 考勤课次 CRUD、学生签到、出勤率统计、预警生成（SSE） |
| `app/api/performance.py` | `/api/performance/...` | 平时分计算与查询、学情诊断（SSE）、班级/学生维度排名 |
| `app/api/announcements.py` | `/api/announcement/...` | 公告 CRUD |

### 新增 Agent 文件（2 个）

| 文件 | 说明 |
|------|------|
| `app/agents/attendance_agent.py` | 考勤预警 Agent：输入缺勤学生数据 → LLM 生成提醒文案 |
| `app/agents/performance_agent.py` | 学情分析 Agent：输入学生多维数据 → LLM 生成诊断 + 建议 |

### 现有文件改动

- `app/agents/orchestrator.py`：路由表扩展 2 个 Agent 关键词
- `app/agents/graph.py`：LangGraph 图新增 2 个节点
- `app/api/courses.py`：班级关联接口
- `app/api/dashboard.py`：看板新增平时分 + 雷达图数据
- `app/main.py`：注册新路由

---

## 七、前端改动

### 新增页面（3 个）

| 页面 | 路由 | 角色 |
|------|------|------|
| `AttendanceView.vue` | `/attendance` | 教师：发起考勤 + 出勤图表 + 预警按钮 |
| `ClassManagementView.vue` | `/classes` | 教师/管理员：班级花名册 + 课程关联 |
| `AnnouncementsView.vue` | `/announcements` | 全角色：公告列表 + 教师发布 |

### 学生端新增

- 考勤签到入口（嵌入"我的作业"附近或独立卡片）
- 学情诊断查看入口

### 现有页面改动

| 页面 | 改动 |
|------|------|
| `DashboardView.vue` | 新增"平时分"指标卡片 + 雷达图 + "学情诊断"按钮（SSE 面板） |
| `StudentsView.vue` | 表格新增平时分列 + 班级筛选下拉 |
| `DashboardLayout.vue` | 导航栏增加：考勤管理、班级管理、公告 |
| `router/index.ts` | 注册 3 条新路由 |

### 可视化图表新增

- 出勤率趋势线（ECharts，复用现有）
- 缺勤排行榜（柱状图）
- 考勤热力图（按周 × 班级）
- 平时分雷达图（四维：出勤/作业/练习/答疑）

---

## 八、论文叙事调整

| 维度 | 修改前 | 修改后 |
|------|--------|--------|
| 系统定位 | 三 Agent 答疑/批改/出题闭环 | 多 Agent 智能课程助教与学情分析系统 |
| Agent 数量 | 3 个 | 5 个 |
| 创新点 | RAG 溯源 + 多 Agent 编排 | + AI 学情诊断 + 考勤预警（人机协同） |
| 对标 | 无明确对标 | 学习通（记录型）vs 本系统（分析型 + Agent 驱动） |

**同步修改**：`框架说明文档.md` 第一段定位 + Agent 列表 + 架构分层图。

---

## 九、工作量评估

| 部分 | 估算 |
|------|------|
| 数据库（6 表 + 迁移 + seed） | 中 |
| 后端（3 API + 2 Agent + 编排改动） | 中 |
| 前端（3 新页面 + 看板改动 + 导航） | 中偏大 |
| 论文/文档叙事 | 小 |
| **整体** | 与之前几轮迭代量级相当，可完成 |

---

## 十、风险与注意事项

1. **`students` 表迁移**：`class_name` → `class_id` 改动影响现有 seed 和查询，需写迁移脚本保证旧数据不丢。
2. **平时分实时性**：物化存储需在考勤/作业/练习变动时刷新，或定时重算——演示环境建议定时重算（简化）。
3. **LLM 依赖**：学情诊断和预警都调云端 API，无 Key 时需有降级提示（与现有答疑一致）。
4. **演示数据量**：考勤和平时分需足够数据量才能让图表好看，seed 脚本要回填近 90 天考勤记录。

---

## 评估确认

请逐项确认或标注修改意见：

- [ ] 功能范围（4 项全做）
- [ ] 班级建模（独立实体 + 双关联）
- [ ] 考勤模型（课次 + 明细双表）
- [ ] 平时分算法（固定权重四维 0.3/0.3/0.2/0.2）
- [ ] 学情分析 Agent（看板内嵌 SSE 诊断）
- [ ] 考勤预警 Agent（教师手动触发 + 确认）
- [ ] Agent 扩充（3 → 5）
- [ ] 论文叙事调整（定位 + 5 Agent）
- [ ] 数据库新增 6 表 + 改 1 表
- [ ] 前端新增 3 页面 + 看板改动

> 确认后我将按此方案开始实现。
